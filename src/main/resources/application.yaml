spring:
  application:
    name: spa-booking

  datasource:
    url: jdbc:sqlserver://localhost:11433;databaseName=HAU;encrypt=true;trustServerCertificate=true
    username: sa
    password: YourStrong@Pass123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        default_schema: spabooking